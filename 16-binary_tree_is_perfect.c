#include "binary_trees.h"

/**
 * binary_tree_is_perfect(const binary_tree_t *tree) - checks if a binary tree
 * is perfect
 * @tree: pointer to the root node of the tree to check
 * Return: Return 1 if perfect, 0 if tree is NULL or tree is not perfect
 */

int binary_tree_is_perfect(const binary_tree_t *tree);
{
	if (!tree)
		return (0);
	else
		int perfect = 0
	return (perfect)
}
